name: Boost Brakeman Scanner
on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
    types:
      - opened
      - synchronize

env:
  test: TRUE
  test2: true
  test3: True
  test4: "true"
  test5: "true"
  test6: ${{ true == true }}
  test7: ${{ env.test == 'true' }}
jobs:
  scanner:
    name: boost
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Execute boost-brakeman
        uses: boostsecurityio/boostsec-scanner-github@v4
        with:
          registry_module: scanners/boostsecurityio/brakeman-scanner
          api_token: ${{ secrets.BOOST_API_TOKEN }}
          api_endpoint: https://api.dev.boostsec.io
          api_enabled: "true"
          log_level: ${{ env.test == true }}
          pre_scan_cmd: ${{ env.test2 == true }}
